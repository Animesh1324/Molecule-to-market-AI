# Pinned to the versions this app is tested against. Unpinned ranges meant a
# rebuild months from now could pull a breaking major release into production.
#
# Requires Python 3.10+ (tested on 3.12). Python 3.9 reached end-of-life in
# October 2025 and cannot install several of the security-patched versions
# below — the local dev venv must be 3.10 or newer.
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.4
sqlalchemy==2.0.52
pydantic==2.13.4
python-dotenv==1.2.3
python-multipart==0.0.32
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5
requests==2.34.2
httpx==0.28.1
anthropic==1.0.0

# Optional: required only when DATABASE_URL points at PostgreSQL.
# psycopg2-binary==2.9.10
